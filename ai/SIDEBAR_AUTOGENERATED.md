# 侧边栏自动生成配置说明

## 📋 配置方式

所有侧边栏现在都使用**自动生成 + 手动链接**的方式：

### 1. 自动生成当前文档实例的文档

```javascript
{
  type: 'autogenerated',
  dirName: '.',  // 从当前文档实例的根目录开始自动生成
}
```

这会自动根据 `docs/{instance}/` 目录下的文件结构生成侧边栏。

### 2. 手动添加通用文档链接

```javascript
{
  type: 'category',
  label: '通用文档',
  items: [
    { type: 'link', label: 'API 文档', href: '/docs/shared/development/apisdk' },
    { type: 'link', label: '认证与安全', href: '/docs/shared/guides/auth_security' },
    { type: 'link', label: 'CLI 工具', href: '/docs/shared/guides/climc' },
    { type: 'link', label: '发布说明', href: '/docs/shared/release-notes' },
  ],
}
```

## 📁 当前配置

### shared.js（共享文档）
```javascript
sharedSidebar: [
  { type: 'autogenerated', dirName: '.' },
]
```
- 自动生成 `docs/shared/` 下的所有文档

### onpremise.js（私有云管理）
```javascript
onpremiseSidebar: [
  { type: 'autogenerated', dirName: '.' },
  {
    type: 'category',
    label: '通用文档',
    items: [
      { type: 'link', label: 'API 文档', href: '/docs/shared/development/apisdk' },
      // ... 其他通用文档链接
    ],
  },
]
```
- 自动生成 `docs/onpremise/` 下的所有文档
- 手动添加通用文档链接

### cmp.js（多云管理）
```javascript
cmpSidebar: [
  { type: 'autogenerated', dirName: '.' },
  {
    type: 'category',
    label: '通用文档',
    items: [
      { type: 'link', label: 'API 文档', href: '/docs/shared/development/apisdk' },
      // ... 其他通用文档链接
    ],
  },
]
```

### baremetal.js（物理机管理）
```javascript
baremetalSidebar: [
  { type: 'autogenerated', dirName: '.' },
  {
    type: 'category',
    label: '通用文档',
    items: [
      { type: 'link', label: 'API 文档', href: '/docs/shared/development/apisdk' },
      // ... 其他通用文档链接
    ],
  },
]
```

## ✅ 优势

1. **无需手动维护**：新增文档会自动出现在侧边栏
2. **结构清晰**：侧边栏结构自动匹配文件结构
3. **文档复用**：通过链接引用通用文档，避免重复
4. **易于扩展**：添加新文档只需创建文件，无需修改配置

## 📝 使用示例

### 添加新文档

只需要在对应的目录下创建文件：

```bash
# 在私有云管理中添加新文档
docs/onpremise/guides/new-feature.md

# 文档会自动出现在侧边栏的 guides 分类下
```

### 控制侧边栏顺序

使用 `sidebar_position` frontmatter 控制文档顺序：

```markdown
---
sidebar_position: 1  # 数字越小越靠前
---

# 文档标题
```

### 隐藏文档

使用 `sidebar_position: null` 或 `draft: true`：

```markdown
---
sidebar_position: null
draft: true
---

# 草稿文档（不会出现在侧边栏）
```

### 自定义分类名称

在目录下创建 `_category_.json` 文件：

```json
{
  "label": "自定义分类名称",
  "position": 1,
  "collapsed": false
}
```

## 🔗 链接到其他文档实例

使用 `link` 类型链接到其他文档实例：

```javascript
{
  type: 'link',
  label: 'API 文档',
  href: '/docs/shared/development/apisdk',  // 完整路径
}
```

## 📚 参考

- [Docusaurus 自动生成侧边栏](https://docusaurus.io/docs/sidebar/autogenerated)
- [Docusaurus 侧边栏配置](https://docusaurus.io/docs/sidebar)

